# Playwright CLI

## Что это такое

**Playwright CLI** - это инструмент командной строки для интерактивного управления браузером через терминал. Он позволяет выполнять действия на веб-страницах, используя команды в терминале без написания кода.

## Установка

```bash
npm install -g @playwright/mcp@latest
```

После установки команда `playwright-cli` становится доступна глобально.

## Основные возможности

### 🎯 Взаимодействие с элементами
- `open <url>` - открыть URL в браузере
- `click <ref>` - кликнуть по элементу
- `dblclick <ref>` - двойной клик
- `type <text>` - ввести текст
- `fill <ref> <text>` - заполнить поле текстом
- `hover <ref>` - навести курсор на элемент
- `drag <startRef> <endRef>` - перетащить элемент

### 📋 Работа с формами
- `select <ref> <val>` - выбрать опцию в dropdown
- `check <ref>` - поставить галочку
- `uncheck <ref>` - снять галочку
- `upload <file>` - загрузить файл

### 🧭 Навигация
- `go-back` - назад
- `go-forward` - вперед
- `reload` - перезагрузить страницу

### ⌨️ Клавиатура
- `press <key>` - нажать клавишу (например, `Enter`, `ArrowLeft`)
- `keydown <key>` - зажать клавишу
- `keyup <key>` - отпустить клавишу

### 🖱️ Мышь
- `mousemove <x> <y>` - переместить курсор
- `mousedown [button]` - нажать кнопку мыши
- `mouseup [button]` - отпустить кнопку мыши
- `mousewheel <dx> <dy>` - прокрутка колесиком

### 📸 Скриншоты и PDF
- `snapshot` - сделать снимок страницы для получения ссылок на элементы
- `screenshot [ref]` - сделать скриншот страницы или элемента
- `pdf` - сохранить страницу как PDF

### 🪟 Вкладки
- `tab-list` - список всех вкладок
- `tab-new [url]` - открыть новую вкладку
- `tab-close [index]` - закрыть вкладку
- `tab-select <index>` - переключиться на вкладку

### 🛠️ Инструменты разработчика
- `console [min-level]` - просмотр сообщений консоли
- `network` - список всех сетевых запросов
- `run-code <code>` - выполнить код Playwright
- `tracing-start` - начать запись трейса
- `tracing-stop` - остановить запись трейса

### 💬 Диалоги
- `dialog-accept [prompt]` - принять диалог
- `dialog-dismiss` - отклонить диалог

### 📏 Окно браузера
- `resize <w> <h>` - изменить размер окна
- `close` - закрыть страницу

### 🔧 Сессии
- `session-list` - список всех сессий
- `session-stop [name]` - остановить сессию
- `session-stop-all` - остановить все сессии
- `session-delete [name]` - удалить данные сессии

## Примеры использования

### Простой сценарий тестирования

```bash
# Открыть страницу
playwright-cli open https://lebed52.github.io/qaed/weekend-checker/
# Сделать снимок для получения ссылок на элементы
playwright-cli snapshot

# Кликнуть по кнопке (используя ref из snapshot)
playwright-cli click "#button-1"

# Заполнить поле ввода
playwright-cli fill "#input-1" "Тестовый текст"

# Нажать Enter
playwright-cli press Enter

# Сделать скриншот результата
playwright-cli screenshot
```

### Работа с формой

```bash
playwright-cli open https://lebed52.github.io/qaed/weekend-checker/
playwright-cli snapshot
playwright-cli fill "#dateInput" "01/01/2026"
playwright-cli click "#checkButton"
playwright-cli screenshot
```

### Работа с несколькими вкладками

```bash
playwright-cli tab-new https://lebed52.github.io/qaed/weekend-checker/
playwright-cli tab-list
playwright-cli tab-select 0
playwright-cli tab-close 1
```

## Отличия от Playwright MCP

### Playwright MCP (Model Context Protocol)

**Что это:**
- Интеграция Playwright в IDE (например, Cursor)
- Работает через AI-ассистента
- Используется через специальные инструменты в контексте разговора

**Как работает:**
- AI-ассистент вызывает функции Playwright через MCP
- Возвращает структурированные результаты
- Встроен в процесс разработки

**Пример использования:**
```
Пользователь: "Открой мой сайт и проверь форму"
AI: [вызывает user-chrome-devtools-navigate_page]
AI: [вызывает user-chrome-devtools-take_snapshot]
AI: [вызывает user-chrome-devtools-fill]
AI: "Форма заполнена, вот результат..."
```

### Playwright CLI

**Что это:**
- Консольный инструмент для терминала
- Интерактивное управление браузером
- Прямой ввод команд

**Как работает:**
- Пользователь вручную вводит команды в терминал
- Каждая команда выполняется отдельно
- Результаты выводятся в консоль

**Пример использования:**
```bash
$ playwright-cli open https://lebed52.github.io/qaed/weekend-checker/
$ playwright-cli click "#submit"
$ playwright-cli screenshot
```

## Сравнительная таблица

| Аспект | Playwright MCP | Playwright CLI |
|--------|----------------|----------------|
| **Интерфейс** | AI-ассистент в IDE | Команды в терминале |
| **Автоматизация** | AI понимает намерения | Нужно знать точные команды |
| **Контекст** | Видит код проекта | Работает независимо |
| **Результаты** | Структурированные, в разговоре | Текст в консоли |
| **Скриншоты** | Встроены в ответ AI | Сохраняются в файл |
| **Использование** | Через естественный язык | Через CLI команды |
| **Интеграция** | Глубокая с IDE | Независимый инструмент |

## Когда использовать что

### Используйте Playwright MCP когда:
- ✅ Работаете в IDE (Cursor)
- ✅ Нужна помощь AI в тестировании
- ✅ Хотите описать задачу на естественном языке
- ✅ Нужна интеграция с кодом проекта
- ✅ Нужны автоматические скриншоты и анализ

### Используйте Playwright CLI когда:
- ✅ Работаете в терминале
- ✅ Нужно быстро проверить что-то вручную
- ✅ Создаете скрипты автоматизации
- ✅ Отлаживаете конкретные действия
- ✅ Работаете без IDE

## Преимущества и недостатки

### Playwright MCP

**Преимущества:**
- 🎯 Понимает намерения через AI
- 🔄 Автоматически находит элементы
- 📊 Анализирует результаты
- 🖼️ Встраивает скриншоты в ответ
- 🧠 Помнит контекст разговора

**Недостатки:**
- ❌ Требует IDE с поддержкой MCP
- ❌ Зависит от AI-сервиса
- ❌ Может неправильно интерпретировать задачу

### Playwright CLI

**Преимущества:**
- ⚡ Быстрый и прямой доступ
- 🎯 Точный контроль команд
- 📝 Легко скриптится
- 🔧 Не требует IDE
- 💾 Независимый инструмент

**Недостатки:**
- ❌ Нужно знать точные команды
- ❌ Нужно вручную находить элементы
- ❌ Нет автоматического анализа
- ❌ Меньше контекста

## Совместное использование

Оба инструмента можно использовать вместе:

**Сценарий:**
1. **Playwright CLI** - быстрая проверка в терминале
2. **Playwright MCP** - анализ проблемы с помощью AI
3. **Playwright CLI** - создание скрипта автоматизации
4. **Playwright MCP** - интеграция в тесты проекта

## Резюме

- **Playwright MCP** = AI + IDE + автоматизация через естественный язык
- **Playwright CLI** = терминал + прямые команды + ручной контроль

Выбирайте инструмент в зависимости от задачи и контекста работы.
