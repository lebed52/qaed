# Полный гайд по DevTools для тестировщиков

## Содержание
1. [Введение](#введение)
2. [Панель Elements](#панель-elements)
3. [Панель Console](#панель-console)
4. [Панель Network](#панель-network)
5. [Панель Application/Storage](#панель-applicationstorage)
6. [Панель Performance](#панель-performance)
7. [Панель Security](#панель-security)
8. [Панель Sources](#панель-sources)
9. [Device Mode](#device-mode)
10. [Продвинутые техники](#продвинутые-техники)

---

## Введение

### Открытие DevTools
- **Windows/Linux**: `F12` или `Ctrl + Shift + I`
- **Mac**: `Cmd + Option + I`
- **Правой кнопкой мыши**: "Inspect Element" / "Исследовать элемент"

### Базовые комбинации
- `Ctrl + Shift + C` / `Cmd + Shift + C` — режим выбора элемента
- `Ctrl + Shift + M` / `Cmd + Shift + M` — режим мобильного устройства
- `Ctrl + ]` / `Ctrl + [` — переключение между панелями

---

## Панель Elements

### 1. Инспектирование элементов

**Что можно проверить:**
- Структуру DOM
- Примененные CSS стили
- Computed styles (вычисленные стили)
- Event listeners (обработчики событий)

**Практическое применение:**

```
Задача: Проверить, почему кнопка не кликается

1. Инспектировать элемент (Ctrl+Shift+C)
2. Проверить:
   - pointer-events: none (блокирует клики)
   - z-index (элемент может быть перекрыт)
   - opacity: 0 или visibility: hidden
   - disabled атрибут
```

### 2. Редактирование HTML на лету

**Как использовать:**
- **Двойной клик** на атрибут — редактировать значение
- **Правая кнопка → Edit as HTML** — редактировать весь блок
- **Delete** — удалить элемент
- **Drag & Drop** — переместить элемент

**Кейсы для тестирования:**

```html
<!-- Тест валидации: изменить type инпута -->
<input type="email"> → изменить на type="text"
Проверить, работает ли валидация email

<!-- Тест максимальной длины -->
<input maxlength="50"> → изменить на maxlength="10000"
Попробовать ввести очень длинный текст

<!-- Тест disabled состояния -->
<button disabled>Submit</button> → удалить атрибут disabled
Проверить, что происходит при отправке формы
```

### 3. Изменение CSS

**Практические проверки:**

```css
/* Тест адаптивности */
.container {
  width: 1200px; /* изменить на 320px */
}

/* Тест overflow контента */
.text-block {
  overflow: hidden; /* изменить на visible */
}

/* Тест видимости элементов */
.modal {
  display: none; /* изменить на block */
}
```

### 4. Computed стили

**Что проверять:**
- Финальные значения всех CSS свойств
- От куда пришло каждое свойство
- Box model (отступы, границы, размеры)

**Проверка box model:**
```
Синий = content
Зеленый = padding
Желтый = border
Оранжевый = margin
```

### 5. Event Listeners

**Как проверить обработчики событий:**

1. Выбрать элемент
2. Открыть вкладку "Event Listeners"
3. Проверить все привязанные события

**Частые проблемы:**
```
❌ Кнопка не работает:
- Нет обработчика click
- Обработчик на родительском элементе (проверить Ancestors)
- Event.preventDefault() блокирует действие

❌ Двойная отправка формы:
- Два обработчика submit на форме
```

### 6. Accessibility (Доступность)

**Вкладка Accessibility в Elements:**

```
Проверяем:
✓ Role (роль элемента)
✓ Name (доступное имя)
✓ Keyboard-focusable (фокус с клавиатуры)
✓ Contrast ratio (контрастность текста)
```

**Пример проверки:**
```html
<!-- Плохо -->
<div onclick="submit()">Send</div>

Проблемы:
- Role: generic (не кнопка)
- Keyboard-focusable: No
- Не работает с screen readers

<!-- Хорошо -->
<button onclick="submit()">Send</button>
```

---

## Панель Console

### 1. Основные команды

**Базовые методы console:**

```javascript
// Вывод информации
console.log('Простое сообщение');
console.info('Информация');
console.warn('Предупреждение');
console.error('Ошибка');

// Группировка логов
console.group('Группа тестов');
console.log('Тест 1');
console.log('Тест 2');
console.groupEnd();

// Таблицы
console.table([
  {name: 'Test 1', status: 'Pass'},
  {name: 'Test 2', status: 'Fail'}
]);

// Таймеры
console.time('Загрузка');
// ... код ...
console.timeEnd('Загрузка'); // Загрузка: 1234ms

// Очистка консоли
console.clear();
```

### 2. Работа с DOM через консоль

**Селекторы:**

```javascript
// $ = querySelector (первый элемент)
$('button'); // первая кнопка на странице
$('#loginBtn'); // элемент с id="loginBtn"
$('.error-message'); // первый элемент с классом

// $$ = querySelectorAll (все элементы)
$$('input'); // все input на странице
$$('.menu-item'); // все элементы с классом menu-item

// $x = XPath селектор
$x('//button[text()="Submit"]'); // кнопка с текстом Submit

// $0 = последний выбранный элемент в Elements
$0.value; // получить значение
$0.click(); // кликнуть
```

**Практические примеры:**

```javascript
// Проверить все обязательные поля
$$('input[required]').forEach(input => {
  console.log(input.name, 'Required:', input.required);
});

// Найти все битые изображения
$$('img').forEach(img => {
  if (!img.complete || img.naturalHeight === 0) {
    console.error('Broken image:', img.src);
  }
});

// Проверить все ссылки
$$('a').forEach(link => {
  if (!link.href || link.href === '#') {
    console.warn('Empty link:', link);
  }
});

// Получить все тексты ошибок валидации
$$('.error-message').map(el => el.textContent);

// Проверить, есть ли элементы вне viewport
$$('*').filter(el => {
  const rect = el.getBoundingClientRect();
  return rect.right < 0 || rect.bottom < 0;
});
```

### 3. Проверка JavaScript переменных

```javascript
// Проверить глобальные переменные
window.userData; // данные пользователя
window.config; // конфигурация

// Проверить localStorage
localStorage;
localStorage.getItem('token');

// Проверить cookies
document.cookie;

// Проверить текущий URL и параметры
location.href;
location.search; // параметры запроса
new URLSearchParams(location.search).get('id');
```

### 4. Monkey Patching для тестирования

**Перехват вызовов функций:**

```javascript
// Перехватить alert
const originalAlert = window.alert;
window.alert = function(msg) {
  console.log('Alert вызван с сообщением:', msg);
  originalAlert(msg);
};

// Перехватить fetch для логирования
const originalFetch = window.fetch;
window.fetch = function(...args) {
  console.log('Fetch вызван:', args[0]);
  return originalFetch(...args);
};

// Заблокировать переход по ссылкам (для тестирования)
document.querySelectorAll('a').forEach(link => {
  link.addEventListener('click', (e) => {
    e.preventDefault();
    console.log('Переход заблокирован:', link.href);
  });
});

// Имитировать медленное соединение
const originalFetch = window.fetch;
window.fetch = async function(...args) {
  await new Promise(resolve => setTimeout(resolve, 3000));
  return originalFetch(...args);
};
```

### 5. Снипеты для частых проверок

**Создание снипета (Sources → Snippets):**

```javascript
// Snippet: Проверка форм
(function checkForms() {
  const forms = document.querySelectorAll('form');
  
  forms.forEach((form, index) => {
    console.group(`Form ${index + 1}`);
    console.log('Action:', form.action);
    console.log('Method:', form.method);
    
    const inputs = form.querySelectorAll('input, textarea, select');
    inputs.forEach(input => {
      console.log(`${input.name}:`, {
        type: input.type,
        required: input.required,
        value: input.value,
        disabled: input.disabled
      });
    });
    
    console.groupEnd();
  });
})();

// Snippet: Проверка производительности изображений
(function checkImages() {
  const images = document.querySelectorAll('img');
  let totalSize = 0;
  
  images.forEach(img => {
    fetch(img.src)
      .then(response => response.blob())
      .then(blob => {
        const size = (blob.size / 1024).toFixed(2);
        totalSize += parseFloat(size);
        
        if (size > 500) {
          console.warn(`Большое изображение (${size}KB):`, img.src);
        }
      });
  });
  
  setTimeout(() => {
    console.log(`Общий размер изображений: ${totalSize.toFixed(2)}KB`);
  }, 2000);
})();

// Snippet: Найти элементы без alt у изображений
(function checkAltTags() {
  const images = $$('img');
  const withoutAlt = images.filter(img => !img.alt);
  
  console.log(`Изображений без alt: ${withoutAlt.length}`);
  withoutAlt.forEach(img => console.warn('Missing alt:', img.src));
})();
```

---

## Панель Network

### 1. Основные возможности

**Что показывает Network:**
- Все HTTP запросы
- Время загрузки ресурсов
- Размер файлов
- Статус коды
- Headers (заголовки)
- Request/Response данные

**Важные колонки:**

```
Name - имя файла/endpoint
Status - HTTP статус код
Type - тип ресурса (document, script, xhr, fetch)
Initiator - что инициировало запрос
Size - размер (transferred/resource)
Time - время выполнения
Waterfall - визуализация времени
```

### 2. Фильтры запросов

**Встроенные фильтры:**

```
All - все запросы
Fetch/XHR - AJAX запросы
JS - JavaScript файлы
CSS - стили
Img - изображения
Media - видео/аудио
Font - шрифты
Doc - HTML документы
WS - WebSocket
```

**Поиск и фильтрация:**

```
// Фильтр по домену
domain:api.example.com

// Фильтр по типу запроса
method:POST

// Фильтр по статусу
status-code:404
status-code:5*  (все 500-ые ошибки)

// Фильтр по размеру
larger-than:100k
smaller-than:50k

// Фильтр по типу ресурса
mime-type:image/png

// Комбинированный фильтр
method:POST domain:api.example.com status-code:200
```

### 3. Анализ запросов API

**Что проверять в каждом запросе:**

```javascript
// 1. General (Общее)
Request URL: https://api.example.com/users
Request Method: POST
Status Code: 200 OK
Remote Address: 192.168.1.1:443

// 2. Response Headers
Content-Type: application/json
Cache-Control: no-cache
Access-Control-Allow-Origin: *
Set-Cookie: session=abc123

// 3. Request Headers
Authorization: Bearer token123
Content-Type: application/json
User-Agent: Mozilla/5.0...

// 4. Request Payload
{
  "email": "test@example.com",
  "password": "123456"
}

// 5. Response
{
  "success": true,
  "user": {
    "id": 1,
    "name": "Test User"
  }
}
```

**Чек-лист для тестирования API:**

```
✓ Статус код корректный (200, 201, 400, 404, 500)
✓ Content-Type соответствует формату (application/json)
✓ Response time приемлемое (< 2s для большинства запросов)
✓ Headers содержат необходимые данные (Authorization)
✓ CORS настроен правильно (Access-Control-Allow-Origin)
✓ Нет утечки чувствительных данных в query parameters
✓ Response содержит ожидаемые поля
✓ Ошибки возвращают понятные сообщения
```

### 4. Копирование запросов

**Copy as...**

```bash
# Copy as cURL (для воспроизведения запроса)
curl 'https://api.example.com/login' \
  -H 'Content-Type: application/json' \
  --data-raw '{"email":"test@test.com","password":"123"}'

# Copy as Fetch (JavaScript код)
fetch("https://api.example.com/login", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
  },
  body: JSON.stringify({email: "test@test.com", password: "123"})
});

# Использование:
# 1. Повторить запрос в терминале (cURL)
# 2. Воспроизвести в автотестах
# 3. Передать разработчикам для багрепорта
```

### 5. Throttling (Симуляция медленного интернета)

**Встроенные профили:**

```
Fast 3G: 
- Download: 1.6 Mbps
- Upload: 750 Kbps
- Latency: 562.5 ms

Slow 3G:
- Download: 400 Kbps
- Upload: 400 Kbps
- Latency: 2000 ms

Offline: нет соединения
```

**Кастомные профили:**

```
Settings → Throttling → Add custom profile

Пример для тестирования:
Name: "Bad Hotel WiFi"
Download: 500 Kbps
Upload: 250 Kbps
Latency: 1000 ms
```

**Что тестировать:**

```
✓ Показываются ли лоадеры/спиннеры
✓ Есть ли timeout для длинных запросов
✓ Корректна ли обработка обрыва соединения
✓ Отображаются ли сообщения об ошибке
✓ Не ломается ли UI при долгой загрузке
✓ Работает ли приложение offline (PWA)
```

### 6. Preserve log и Disable cache

```
☑ Preserve log
- Сохраняет логи при переходах между страницами
- Полезно для отслеживания редиректов
- Использовать при тестировании многошаговых процессов

☑ Disable cache
- Отключает кеширование браузера
- Всегда загружает свежие версии файлов
- Обязательно включать при тестировании изменений
```

### 7. Блокировка запросов (Request blocking)

**Как использовать:**

```
Network → правой кнопкой на запрос → Block request URL
или
Network → Block request patterns

Паттерны:
*analytics* - блокировать все запросы с analytics в URL
*.js - блокировать все JS файлы
*/ads/* - блокировать рекламу
```

**Кейсы для тестирования:**

```javascript
// Тест: Что будет, если не загрузится Google Analytics
Block: *google-analytics.com*

// Тест: Работает ли сайт без сторонних скриптов
Block: *cdn.jsdelivr.net*

// Тест: Как отображается сайт без изображений
Block: *.jpg, *.png, *.gif

// Тест: Отображение ошибок при падении API
Block: */api/*

// Тест: Поведение при недоступности авторизации
Block: */auth/login*
```

### 8. Экспорт и импорт HAR

**HAR (HTTP Archive):**

```
Экспорт: Network → правой кнопкой → Save as HAR with content

Использование:
1. Отправить разработчикам для анализа
2. Загрузить в онлайн-анализаторы (webpagetest.org)
3. Сравнить производительность разных версий
4. Документирование багов с проблемами network
```

### 9. Типичные проблемы и как их найти

```javascript
// ❌ Проблема: Дублирование запросов
Признаки: один и тот же запрос выполняется несколько раз
Поиск: отсортировать по Name, найти одинаковые
Причина: баг в коде (useEffect без dependencies в React)

// ❌ Проблема: N+1 запросы
Признаки: много похожих запросов подряд
Пример: /api/user/1, /api/user/2, /api/user/3...
Решение: использовать bulk запросы

// ❌ Проблема: Большие payload
Поиск: отсортировать по Size, проверить большие
Проблема: отправка лишних данных
Пример: отправка всей формы вместо измененных полей

// ❌ Проблема: Медленные запросы
Поиск: отсортировать по Time
Порог: > 3 секунды для API
Причины: нет индексов в БД, N+1, тяжелые вычисления

// ❌ Проблема: Не закешированные ресурсы
Признаки: Status 200 вместо 304 для статики
Проверка: Response Headers → Cache-Control
Должно быть: Cache-Control: max-age=31536000 для статики

// ❌ Проблема: CORS ошибки
Признаки: 
- Status: (failed) или CORS error
- В Console: "CORS policy blocked..."
Проверка: Response Headers → Access-Control-Allow-Origin
```

---

## Панель Application/Storage

### 1. Local Storage

**Просмотр и редактирование:**

```javascript
// В Application → Local Storage → выбрать домен

Структура:
Key          | Value
-------------|------------------
auth_token   | eyJhbGc...
user_name    | John Doe
theme        | dark
language     | en

// Через Console:
localStorage.getItem('auth_token');
localStorage.setItem('test_key', 'test_value');
localStorage.removeItem('auth_token');
localStorage.clear(); // удалить все
```

**Тестовые сценарии:**

```javascript
// Тест 1: Проверка авторизации после удаления токена
1. Удалить ключ 'auth_token'
2. Обновить страницу
3. Ожидаемо: редирект на /login

// Тест 2: Проверка лимита localStorage (обычно ~5-10MB)
let data = 'a'.repeat(1024 * 1024); // 1MB
for(let i = 0; i < 20; i++) {
  try {
    localStorage.setItem(`key_${i}`, data);
  } catch(e) {
    console.log('Limit reached at:', i, 'MB');
  }
}

// Тест 3: Проверка XSS через localStorage
localStorage.setItem('user_name', '<script>alert("XSS")</script>');
// Проверить, экранируется ли вывод

// Тест 4: Изменение настроек пользователя
localStorage.setItem('language', 'fr');
location.reload(); // проверить смену языка
```

### 2. Session Storage

**Отличия от Local Storage:**

```
Local Storage:
- Данные сохраняются навсегда
- Общие для всех вкладок одного домена

Session Storage:
- Данные удаляются при закрытии вкладки
- Уникальны для каждой вкладки
```

**Использование:**

```javascript
// Application → Session Storage

Тест: Проверка изоляции между вкладками
1. Открыть сайт в двух вкладках
2. В первой: sessionStorage.setItem('tab', 'tab1')
3. Во второй: sessionStorage.setItem('tab', 'tab2')
4. Проверить, что значения разные
```

### 3. Cookies

**Просмотр cookies:**

```
Application → Cookies → выбрать домен

Столбцы:
Name        | session_id
Value       | abc123xyz
Domain      | .example.com
Path        | /
Expires     | 2024-12-31
Size        | 50
HttpOnly    | ✓
Secure      | ✓
SameSite    | Lax
Priority    | Medium
```

**Важные атрибуты для безопасности:**

```javascript
// HttpOnly: cookie недоступна через JavaScript
// Тест: попробовать прочитать
document.cookie; // не должна быть видна

// Secure: отправляется только по HTTPS
// Проверка: в prod должна быть включена для auth

// SameSite: защита от CSRF
// Strict - только с того же сайта
// Lax - разрешает GET с других сайтов
// None - нет ограничений (требует Secure)
```

**Тестовые сценарии:**

```javascript
// Тест 1: Удаление сессии
1. Удалить cookie 'session_id'
2. Обновить страницу
3. Должен быть logout

// Тест 2: Изменение роли пользователя (если в cookie)
1. Найти cookie типа 'user_role'
2. Изменить значение с 'user' на 'admin'
3. Проверить, даст ли доступ к админке
4. ⚠️ Если дало - критическая уязвимость!

// Тест 3: Проверка истечения cookie
1. Установить Expires на прошедшую дату
2. Обновить страницу
3. Cookie должна исчезнуть

// Создание тестовых cookie:
document.cookie = "test=value; path=/; max-age=3600";
```

### 4. IndexedDB

**Для сложных данных:**

```javascript
// Application → IndexedDB → выбрать базу

Структура:
Database: myAppDB
  Object Store: users
    Key: 1
    Value: {id: 1, name: "John", email: "john@example.com"}
    
  Object Store: posts
    Key: 1
    Value: {id: 1, title: "Hello", content: "..."}
```

**Работа через Console:**

```javascript
// Открыть базу
let request = indexedDB.open('myAppDB');

request.onsuccess = function(event) {
  let db = event.target.result;
  
  // Прочитать данные
  let transaction = db.transaction(['users'], 'readonly');
  let objectStore = transaction.objectStore('users');
  let getRequest = objectStore.get(1);
  
  getRequest.onsuccess = function() {
    console.log('User:', getRequest.result);
  };
};

// Быстрое удаление всей базы
indexedDB.deleteDatabase('myAppDB');
```

**Тестовые кейсы:**

```javascript
// Проверка работы offline-режима (PWA)
1. Отключить Network (Offline)
2. Обновить страницу
3. Проверить, загружаются ли данные из IndexedDB

// Проверка синхронизации
1. Изменить данные в IndexedDB вручную
2. Проверить, синхронизируются ли они с сервером
```

### 5. Cache Storage (Service Workers)

**Для PWA приложений:**

```
Application → Cache Storage → выбрать кэш

Кэши:
- precache-v1 (статические файлы)
- api-cache-v1 (API responses)
- images-v1 (изображения)
```

**Проверка кэша:**

```javascript
// Список всех кэшей
caches.keys().then(names => console.log(names));

// Содержимое кэша
caches.open('precache-v1').then(cache => {
  cache.keys().then(requests => {
    requests.forEach(request => console.log(request.url));
  });
});

// Очистить все кэши
caches.keys().then(names => {
  names.forEach(name => caches.delete(name));
});
```

**Тесты:**

```javascript
// Тест offline работы
1. Загрузить страницу онлайн
2. Network → Offline
3. Обновить страницу
4. Страница должна загрузиться из кэша

// Тест обновления кэша
1. Очистить Cache Storage
2. Обновить страницу
3. Проверить, что кэш заполнился снова
```

### 6. Manifest (PWA)

**Проверка Web App Manifest:**

```json
{
  "name": "My App",
  "short_name": "App",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#000000",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

**Чек-лист:**

```
✓ Manifest.json корректно подключен
✓ Все иконки доступны (не 404)
✓ start_url работает
✓ display: standalone для установки как app
✓ theme_color соответствует дизайну
✓ Icons разных размеров присутствуют (192, 512)
```

### 7. Service Workers

**Проверка и отладка:**

```
Application → Service Workers

Информация:
- Status: activated and running
- Source: /sw.js
- Scope: /

Действия:
[Update] - обновить SW
[Unregister] - удалить регистрацию
[Bypass for network] - игнорировать SW
[Update on reload] - авто-обновление
[Offline] - эмуляция offline
```

**Типичные проблемы:**

```javascript
// Проблема: старая версия кэша
Решение:
1. Update on reload
2. Shift + F5 (hard reload)
3. Unregister → обновить страницу

// Проблема: Service Worker не обновляется
Проверить:
- Cache-Control headers на sw.js (должен быть max-age=0)
- Изменилось ли содержимое sw.js

// Тест обновления приложения
1. Изменить версию кэша в sw.js
2. Application → Service Workers → Update
3. Проверить, что новые файлы загружены
```

### 8. Очистка всех данных

**Clear site data:**

```
Application → Storage → Clear site data

Опции:
☑ Local and session storage
☑ IndexedDB
☑ Web SQL (устарело)
☑ Cookies
☑ Cache storage

[Clear site data] - удалить все выбранное
```

**Когда использовать:**

```
✓ Тест первого запуска приложения
✓ Проверка чистой установки
✓ Тест после logout
✓ Сброс к дефолтным настройкам
```

---

## Панель Performance

### 1. Запись производительности

**Как использовать:**

```
1. Performance → кнопка Record (●)
2. Выполнить действия на странице
3. Stop recording
4. Анализ результатов
```

**Или:**

```
1. Reload and record (⟳) - запись при загрузке
2. Страница перезагрузится и запишется процесс загрузки
```

### 2. Анализ результатов

**Основные метрики:**

```javascript
// FCP (First Contentful Paint)
Когда: появляется первый контент
Хорошо: < 1.8s

// LCP (Largest Contentful Paint)
Когда: отображается самый большой элемент
Хорошо: < 2.5s

// TTI (Time to Interactive)
Когда: страница становится интерактивной
Хорошо: < 3.8s

// TBT (Total Blocking Time)
Время блокировки главного потока
Хорошо: < 200ms

// CLS (Cumulative Layout Shift)
Смещение элементов при загрузке
Хорошо: < 0.1
```

**Секции timeline:**

```
Network: загрузка ресурсов
Frames: отрисовка кадров (60 FPS = зеленый)
Timings: метрики (FCP, LCP, DCL, Load)
Main: JavaScript выполнение
Raster: растеризация
GPU: работа GPU
```

### 3. Поиск проблем производительности

**Long Tasks (долгие задачи):**

```
Признаки:
- Красные треугольники в Main
- Задачи > 50ms

Как найти:
1. Кликнуть на красную зону
2. Bottom-Up → посмотреть Self Time
3. Найти функцию с большим временем

Пример проблемы:
Function: processLargeArray
Self Time: 850ms
Решение: разбить на части, использовать Web Workers
```

**Layout Thrashing (принудительный reflow):**

```javascript
// Плохо (вызывает layout на каждой итерации)
elements.forEach(el => {
  el.style.width = el.offsetWidth + 10 + 'px'; // Read + Write
});

// Хорошо (batch операции)
const widths = elements.map(el => el.offsetWidth); // Read
elements.forEach((el, i) => {
  el.style.width = widths[i] + 10 + 'px'; // Write
});

Как найти в DevTools:
Performance → Main → найти повторяющиеся Layout
```

**Memory Leaks (утечки памяти):**

```
1. Performance → Enable memory checkbox
2. Запись с памятью
3. График Memory должен стабилизироваться
4. Если растет постоянно - утечка

Частые причины:
- Забытые event listeners
- Глобальные переменные
- Замыкания с большими данными
- Таймеры без clearTimeout
```

### 4. Анализ сети

**Coverage (покрытие кода):**

```
More tools → Coverage → Reload

Показывает:
- URL ресурса
- Type (JS/CSS)
- Total Bytes
- Unused Bytes (% красным)

Действия:
- Найти файлы с >50% неиспользуемого кода
- Разделить на chunks
- Использовать code splitting
- Удалить неиспользуемые библиотеки
```

### 5. Rendering Performance

**Show paint rectangles:**

```
More tools → Rendering → Paint flashing

Использование:
1. Включить Paint flashing
2. Взаимодействовать со страницей
3. Зеленые прямоугольники = перерисовка

Проблема: постоянное мигание без действий
Причина: анимации, автообновление, курсор мыши effect
```

**Frame rendering stats:**

```
More tools → Rendering → Frame Rendering Stats

Показывает:
- Текущий FPS
- GPU memory usage

Хорошо: стабильные 60 FPS
Плохо: < 30 FPS, большие скачки
```

### 6. Симуляция медленных устройств

**CPU throttling:**

```
Performance → Settings (⚙️) → CPU

Варианты:
- No throttling
- 4x slowdown (хорошие смартфоны)
- 6x slowdown (средние смартфоны)
- 20x slowdown (слабые устройства)

Использование:
1. Включить 6x slowdown
2. Выполнить тяжелые операции
3. Проверить отзывчивость UI
```

### 7. Практические сценарии

**Сценарий 1: Медленная загрузка страницы**

```
1. Performance → Reload and record
2. Проверить метрики:
   - FCP > 3s → оптимизировать критический путь
   - LCP > 4s → оптимизировать большие изображения
3. Network waterfall:
   - Много последовательных запросов → preload
   - Большие файлы → сжатие, lazy loading
```

**Сценарий 2: Лаги при скролле**

```
1. Performance → Record
2. Скроллить страницу
3. Frames → найти dropped frames (пропущенные кадры)
4. Main → искать Long Tasks во время скролла
5. Частые причины:
   - Тяжелые scroll listeners
   - Синхронные Layout/Paint в onScroll
   - Слишком много DOM элементов
```

**Сценарий 3: Долгая инициализация приложения**

```
1. Performance → Reload and record
2. Main thread → искать большие желтые блоки (JS execution)
3. Bottom-Up → сортировать по Self Time
4. Оптимизация:
   - Ленивая загрузка модулей
   - Code splitting
   - Перенос в Web Workers
```

---

## Панель Security

### 1. Обзор безопасности

**Security Overview:**

```
Security → View security details

Проверяет:
✓ HTTPS соединение
✓ Сертификат SSL/TLS
✓ Mixed content (HTTP на HTTPS странице)
✓ Certificate transparency
```

**Информация о сертификате:**

```
Certificate:
- Issued to: example.com
- Issued by: Let's Encrypt
- Valid from: 2024-01-01
- Valid until: 2024-12-31
- Protocol: TLS 1.3
- Key exchange: X25519
- Cipher: AES_128_GCM
```

### 2. Mixed Content

**Типы Mixed Content:**

```javascript
// Активный Mixed Content (блокируется браузером)
<script src="http://example.com/script.js"></script>
<link href="http://example.com/style.css">
<iframe src="http://example.com"></iframe>

// Пассивный Mixed Content (предупреждение)
<img src="http://example.com/image.jpg">
<audio src="http://example.com/audio.mp3">
<video src="http://example.com/video.mp4">

Проблемы:
❌ Снижает безопасность
❌ Браузеры блокируют
❌ Предупреждения для пользователей

Решение:
✓ Использовать HTTPS для всех ресурсов
✓ Или protocol-relative URLs: //example.com/script.js
✓ Или удалить протокол: src="/script.js"
```

### 3. Проверка заголовков безопасности

**Content Security Policy (CSP):**

```
Application → Network → Headers

Content-Security-Policy: 
default-src 'self'; 
script-src 'self' 'unsafe-inline' https://trusted.cdn.com; 
style-src 'self' 'unsafe-inline';
img-src 'self' data: https:;

Что проверять:
✓ Присутствует ли CSP заголовок
✓ Нет ли 'unsafe-inline' (опасно для XSS)
✓ Нет ли 'unsafe-eval' (опасно)
✓ Ресурсы из белого списка
```

**Другие важные заголовки:**

```http
# Запрет встраивания в iframe (защита от clickjacking)
X-Frame-Options: DENY
или
Content-Security-Policy: frame-ancestors 'none'

# Включение XSS защиты браузера
X-XSS-Protection: 1; mode=block

# Запрет MIME-sniffing
X-Content-Type-Options: nosniff

# Принудительный HTTPS
Strict-Transport-Security: max-age=31536000; includeSubDomains

# Контроль Referrer
Referrer-Policy: no-referrer-when-downgrade
```

### 4. Тестирование безопасности через DevTools

**Тест 1: XSS (Cross-Site Scripting)**

```javascript
// В форме ввода попробовать:
<script>alert('XSS')</script>
<img src=x onerror="alert('XSS')">
<svg onload="alert('XSS')">

// Проверить:
1. Elements → посмотреть, как отображается в DOM
2. Если скрипт выполнился → уязвимость!
3. Должно быть: HTML entities escaped
   < становится &lt;
   > становится &gt;
```

**Тест 2: Проверка cookies на безопасность**

```javascript
// Application → Cookies

Должно быть для session/auth cookies:
✓ HttpOnly: true (защита от XSS)
✓ Secure: true (только HTTPS)
✓ SameSite: Strict или Lax (защита от CSRF)

// Тест в Console:
document.cookie; // auth cookies не должны быть видны
```

**Тест 3: localStorage с чувствительными данными**

```javascript
// Application → Local Storage

❌ НЕ должно храниться:
- Пароли
- Токены (лучше в httpOnly cookie)
- Номера карт
- Персональные данные в открытом виде

✓ Можно хранить:
- Настройки UI
- Непубличные ID
- Зашифрованные данные
```

**Тест 4: CORS**

```javascript
// Попытка запроса к API с другого домена

fetch('https://api.example.com/data')
  .then(r => r.json())
  .then(console.log)
  .catch(console.error);

Проверка в Network:
Response Headers:
Access-Control-Allow-Origin: *  // ❌ Слишком открыто
Access-Control-Allow-Origin: https://mysite.com  // ✓ OK
Access-Control-Allow-Credentials: true  // Проверить необходимость
```

---

## Панель Sources

### 1. Файловая структура

**Навигация:**

```
Sources → Page

Структура:
📁 top (главный домен)
  📁 css
    📄 styles.css
  📁 js
    📄 app.js
    📄 vendor.js
  📁 images
📁 cdn.example.com (CDN)
  📄 jquery.min.js
```

### 2. Debugging JavaScript

**Breakpoints (точки останова):**

```javascript
// 1. Обычный breakpoint
Клик на номер строки в Sources

// 2. Conditional breakpoint
Правой кнопкой → Add conditional breakpoint
Условие: userId === 123

// 3. Logpoint (вывод без остановки)
Правой кнопкой → Add logpoint
Сообщение: User ID: {userId}, Name: {userName}

// 4. Из кода
debugger; // остановка выполнения

// 5. XHR/Fetch breakpoints
Sources → XHR/fetch Breakpoints → +
URL contains: /api/login
```

**Управление выполнением:**

```
F8 или ▶️ - продолжить выполнение (Resume)
F10 или ⤵️ - следующая строка (Step over)
F11 или ⬇️ - войти в функцию (Step into)
Shift+F11 или ⬆️ - выйти из функции (Step out)
Ctrl+Shift+E - выполнить выделенный код
```

**Практический пример:**

```javascript
// Код с багом
function calculateTotal(items) {
  let total = 0;
  
  items.forEach(item => {
    total += item.price * item.quantity; // поставить breakpoint
  });
  
  return total;
}

// Отладка:
1. Breakpoint на строке с total +=
2. Запустить функцию
3. При остановке:
   - Scope → посмотреть значения переменных
   - Watch → добавить выражения: item.price, item.quantity
   - Console → вычислить: item.price * item.quantity
4. F10 для следующей итерации
```

### 3. Scope и Watch

**Scope (область видимости):**

```javascript
function outer() {
  let outerVar = 'outer';
  
  function inner() {
    let innerVar = 'inner';
    debugger; // остановка здесь
  }
  
  inner();
}

В Scope при остановке:
📁 Local
  innerVar: "inner"
📁 Closure (outer)
  outerVar: "outer"
📁 Global
  window: Window
```

**Watch (наблюдение за выражениями):**

```
Sources → Watch → + Add expression

Примеры:
user.email
items.length
items.filter(i => i.price > 100).length
localStorage.getItem('token')
JSON.parse(response)
```

### 4. Call Stack

**Трассировка вызовов:**

```javascript
function a() {
  b();
}

function b() {
  c();
}

function c() {
  debugger; // остановка
}

a();

Call Stack при остановке:
c @ app.js:11
b @ app.js:7
a @ app.js:3
(anonymous) @ app.js:15
```

**Использование:**

```
✓ Понять, как была вызвана функция
✓ Найти источник ошибки
✓ Кликнуть на функцию в стеке → перейти к её коду
```

### 5. Event Listener Breakpoints

**Типы:**

```
Sources → Event Listener Breakpoints

Категории:
☑ Mouse
  ☐ click
  ☐ mousedown
  ☐ mouseover
☑ Keyboard
  ☐ keydown
  ☐ keypress
☑ Form
  ☐ submit
  ☐ change
☑ DOM Mutation
  ☐ Node added
  ☐ Node removed
```

**Кейсы:**

```javascript
// Случай 1: Неизвестно, где обработчик клика
1. Включить Mouse → click
2. Кликнуть на элемент
3. Остановка на обработчике

// Случай 2: Форма не отправляется
1. Включить Form → submit
2. Отправить форму
3. Найти preventDefault()

// Случай 3: Элемент пропадает
1. Включить DOM Mutation → Node removed
2. Когда элемент удалится → остановка
3. Call Stack покажет, кто удалил
```

### 6. Overrides (Локальные изменения)

**Как использовать:**

```
1. Sources → Overrides → Select folder
2. Разрешить доступ к папке
3. Внести изменения в любой файл
4. Сохранить (Ctrl+S)
5. Изменения сохранятся локально
6. При перезагрузке будут применяться

Использование:
✓ Тестирование фиксов без деплоя
✓ Проверка разных сценариев
✓ Отладка продакшена
```

**Пример:**

```javascript
// Оригинал app.js:
function submit() {
  // ... код с багом
}

// В Sources → app.js → исправить баг → Ctrl+S
function submit() {
  // ... исправленный код
}

// Теперь при перезагрузке используется локальная версия
```

### 7. Snippets (Снипеты)

**Сохраненные скрипты:**

```javascript
// Sources → Snippets → + New snippet

// Snippet: Check All Forms
(function() {
  const forms = document.querySelectorAll('form');
  console.table(Array.from(forms).map((form, i) => ({
    index: i,
    action: form.action,
    method: form.method,
    inputs: form.querySelectorAll('input').length
  })));
})();

// Запуск: Ctrl+Enter или правой кнопкой → Run
```

**Полезные снипеты для тестирования:**

```javascript
// 1. Найти все disabled элементы
$$('[disabled]').forEach(el => {
  console.log(el, 'is disabled');
  el.removeAttribute('disabled');
});

// 2. Подсветить все required поля
$$('[required]').forEach(el => {
  el.style.border = '2px solid red';
});

// 3. Заполнить форму тестовыми данными
$('#email').value = 'test@example.com';
$('#password').value = 'Test123!';
$('#name').value = 'Test User';
$('#phone').value = '+1234567890';

// 4. Показать все скрытые элементы
$$('[style*="display: none"], [style*="visibility: hidden"]').forEach(el => {
  el.style.display = 'block';
  el.style.visibility = 'visible';
  el.style.border = '2px solid orange';
});

// 5. Логировать все AJAX запросы
(function() {
  const origOpen = XMLHttpRequest.prototype.open;
  XMLHttpRequest.prototype.open = function() {
    console.log('XHR:', arguments[0], arguments[1]);
    origOpen.apply(this, arguments);
  };
})();

// 6. Проверить доступность (a11y)
$$('img:not([alt])').length; // изображения без alt
$$('a:not([href])').length; // ссылки без href
$$('button:not([aria-label]):not(:has(*))').filter(btn => !btn.textContent.trim()).length; // кнопки без текста
```

### 8. Blackboxing Scripts

**Игнорирование сторонних скриптов:**

```
Settings → Blackboxing → Add pattern

Паттерны:
node_modules/*
webpack/*
jquery\.min\.js
*/analytics/*

Эффект:
- Пропускаются при debugging
- Не показываются в Call Stack
- Ошибки не останавливают выполнение
```

---

## Device Mode

### 1. Основы Device Mode

**Включение:**

```
Ctrl+Shift+M или кнопка устройства в toolbar

Возможности:
- Эмуляция разных устройств
- Изменение viewport
- Поворот экрана
- Throttling сети и CPU
- Touch events
- Geolocation
- Sensors
```

### 2. Preset устройства

**Популярные устройства:**

```
Мобильные:
- iPhone SE (375 x 667)
- iPhone 12 Pro (390 x 844)
- iPhone 14 Pro Max (430 x 932)
- Pixel 5 (393 x 851)
- Samsung Galaxy S20 (360 x 800)

Планшеты:
- iPad Mini (768 x 1024)
- iPad Pro (1024 x 1366)
- Surface Pro 7 (912 x 1368)

Responsive:
- Кастомный размер (изменяется мышью)
```

### 3. Кастомные устройства

**Добавление своего устройства:**

```
Settings → Devices → Add custom device

Параметры:
- Device Name: Custom Phone
- Width: 375
- Height: 812
- Device pixel ratio: 3
- User agent string: Mozilla/5.0...
- Device type: Mobile
```

### 4. Touch Events

**Эмуляция касаний:**

```
Device Mode → ... → Add device pixel ratio

Режимы:
1. Device-based - автоматически для мобильных
2. Force enabled - всегда включено

Тестирование:
- Swipe жесты
- Pinch zoom
- Long press
- Touch targets (минимум 48x48px)
```

**Проверка touch-friendly:**

```javascript
// В Console:
// Найти маленькие кликабельные элементы
Array.from($$('a, button')).filter(el => {
  const rect = el.getBoundingClientRect();
  return rect.width < 48 || rect.height < 48;
}).forEach(el => {
  el.style.outline = '2px solid red';
  console.warn('Элемент слишком маленький:', el, rect.width, rect.height);
});
```

### 5. Sensor эмуляция

**Geolocation:**

```
... (меню) → Sensors → Location

Варианты:
- No override
- Berlin
- London
- Moscow
- San Francisco
- Tokyo
- Custom location (ввод координат)

Тестирование:
navigator.geolocation.getCurrentPosition(pos => {
  console.log('Lat:', pos.coords.latitude);
  console.log('Lon:', pos.coords.longitude);
});
```

**Orientation (ориентация устройства):**

```
Sensors → Orientation

Варианты:
- Portrait (вертикально)
- Landscape (горизонтально)
- Custom (свои углы)

API для тестирования:
window.addEventListener('deviceorientation', event => {
  console.log('Alpha:', event.alpha);
  console.log('Beta:', event.beta);
  console.log('Gamma:', event.gamma);
});
```

### 6. User Agent

**Изменение User Agent:**

```
Network conditions → User agent

Варианты:
- Browser default
- Android
- iPhone
- iPad
- Desktop
- Custom

Зачем:
✓ Тестирование мобильной/десктопной версии
✓ Проверка feature detection
✓ Тестирование бота (Googlebot)
```

**Пример:**

```javascript
// Проверить текущий UA
navigator.userAgent;

// Установить кастомный
// Network conditions → Custom → ввести UA
// Например: Googlebot/2.1

// Проверить, что изменилось
navigator.userAgent; // теперь Googlebot
```

### 7. Viewport и медиа-запросы

**Responsive design testing:**

```
Device Mode → Responsive

Быстрые размеры:
- Mobile S: 320px
- Mobile M: 375px
- Mobile L: 425px
- Tablet: 768px
- Laptop: 1024px
- Laptop L: 1440px
- 4K: 2560px
```

**Show media queries:**

```
... → Show media queries

Визуализация:
Синий = max-width
Зеленый = min-width
Оранжевый = min/max width

Клик на полосу = установить этот размер
```

**Проверка breakpoints:**

```css
/* style.css */
@media (max-width: 768px) {
  .menu {
    display: none; /* должно скрываться */
  }
}

Тест:
1. Установить width: 769px → меню видно
2. Установить width: 768px → меню скрыто
3. Проверить граничные значения
```

### 8. Тестовые сценарии

**Сценарий 1: Полный тест адаптивности**

```
Чек-лист:
□ 320px (iPhone SE) - минимальная ширина
  □ Текст читается
  □ Нет горизонтального скролла
  □ Кнопки кликабельны (>48px)
  □ Форма заполняется
  
□ 375px (iPhone X)
  □ Навигация доступна
  □ Изображения масштабируются
  
□ 768px (iPad)
  □ Переход к планшетной версии
  □ Две колонки где нужно
  
□ 1024px+ (Desktop)
  □ Полная десктопная версия
  □ Все элементы на месте
  
□ Landscape режим
  □ Не ломается при повороте
  □ Адекватное использование пространства
```

**Сценарий 2: Touch-friendly проверка**

```javascript
// Snippet для проверки
(function checkTouchTargets() {
  const minSize = 48;
  const interactive = $$('a, button, input, select, textarea, [onclick]');
  
  const tooSmall = interactive.filter(el => {
    const rect = el.getBoundingClientRect();
    return rect.width < minSize || rect.height < minSize;
  });
  
  console.log(`Найдено ${tooSmall.length} элементов меньше ${minSize}px`);
  
  tooSmall.forEach(el => {
    el.style.outline = '3px solid red';
    const rect = el.getBoundingClientRect();
    console.warn(`${el.tagName} ${rect.width}x${rect.height}`, el);
  });
})();
```

**Сценарий 3: Производительность на мобильном**

```
1. Device Mode → iPhone 12 Pro
2. ... → Add device pixel ratio → 3x
3. Network → Slow 3G
4. Performance → CPU: 4x slowdown
5. Reload and record
6. Проверить:
   □ FCP < 3s
   □ TTI < 5s
   □ No jank при скролле
```

---

## Продвинутые техники

### 1. Command Menu

**Быстрый доступ ко всему:**

```
Ctrl+Shift+P (Cmd+Shift+P на Mac)

Полезные команды:

> Screenshot
- Capture full size screenshot
- Capture node screenshot (выбранного элемента)
- Capture screenshot (видимой области)

> Coverage
- Show Coverage (неиспользуемый код)

> Rendering
- Show Rendering (опции рендеринга)

> Sensors
- Show Sensors (geolocation, orientation)

> Network
- Block request URL
- Network conditions

> Appearance
- Switch to dark theme
- Switch to light theme

> Disable JavaScript
- Тестирование без JS
```

### 2. Workspace (Рабочее пространство)

**Редактирование файлов напрямую:**

```
Sources → Filesystem → + Add folder to workspace

Возможности:
✓ Редактировать локальные файлы
✓ Изменения сохраняются на диск
✓ Синхронизация с сервером разработки
✓ Автообновление при изменениях

Использование:
1. Добавить папку проекта
2. Разрешить доступ
3. Файлы с зеленой точкой = синхронизированы
4. Изменения в DevTools = изменения на диске
```

### 3. Layers (Слои рендеринга)

**Анализ композитных слоёв:**

```
More tools → Layers

Показывает:
- Композитные слои
- Paint time
- Memory usage
- Причину создания слоя

Проверка:
□ Слишком много слоёв (>50) - перерасход памяти
□ Большие слои - медленный рендеринг
□ Ненужные слои - убрать transform/opacity
```

**Создание слоя специально:**

```css
.animated-element {
  /* Создаст отдельный слой */
  will-change: transform;
  /* или */
  transform: translateZ(0);
}

Зачем:
✓ Плавные анимации
✓ Избежать repaints других элементов
✓ Использование GPU

Но:
❌ Больше памяти
❌ Медленнее на слабых устройствах
```

### 4. Animations Inspector

**Отладка анимаций:**

```
More tools → Animations

Возможности:
- Визуализация всех анимаций
- Замедление анимаций (25%, 10%)
- Пауза/воспроизведение
- Изменение timing function
- Scrubbing (перемотка)

Использование:
1. Запустить анимацию
2. Animations panel покажет timeline
3. Можно замедлить для анализа
4. Кликнуть для редактирования
```

**Проверка производительности анимаций:**

```css
/* ✓ Хорошо - используют GPU */
.element {
  animation: slide 1s;
}

@keyframes slide {
  from { transform: translateX(0); }
  to { transform: translateX(100px); }
}

/* ❌ Плохо - вызывают layout */
@keyframes slide-bad {
  from { left: 0; }
  to { left: 100px; }
}

Проверка в Performance:
- Зеленые линии в Frames = 60 FPS ✓
- Красные треугольники = jank ❌
```

### 5. Memory Profiler

**Поиск утечек памяти:**

```
Memory → Heap snapshot → Take snapshot

Сценарий поиска утечки:
1. Snapshot 1 (начальное состояние)
2. Выполнить действие (открыть модал)
3. Закрыть модал
4. Snapshot 2
5. Snapshot 3 (после сборки мусора)
6. Comparison: Snapshot 3 vs Snapshot 1

Что искать:
- Detached DOM nodes (оторванные узлы)
- Большие массивы/объекты
- Event listeners на удаленных элементах
```

**Allocation Timeline:**

```
Memory → Allocation instrumentation on timeline → Start

Показывает:
- Когда выделяется память
- Что выделяет
- Синие линии вверх = выделение
- Синие линии вниз = освобождение

Утечка = постоянный рост без освобождения
```

### 6. Multi-device Testing

**Remote Debugging (Android):**

```
Подключение:
1. Включить USB debugging на Android
2. Подключить к компьютеру
3. Chrome → chrome://inspect
4. Выбрать устройство
5. Inspect → полноценный DevTools

Возможности:
✓ Инспекция на реальном устройстве
✓ Все возможности DevTools
✓ Скриншоты реального экрана
✓ Port forwarding (localhost на устройстве)
```

**iOS Debugging (Safari):**

```
Подключение:
1. iPhone → Settings → Safari → Advanced → Web Inspector
2. Mac → Safari → Preferences → Advanced → Show Develop menu
3. Подключить iPhone к Mac
4. Safari → Develop → iPhone → выбрать страницу

Ограничения:
- Только на Mac
- Только Safari DevTools
- Меньше возможностей чем Chrome
```

### 7. Protocol Monitor

**Low-level debugging:**

```
More tools → Protocol monitor

Показывает:
- Chrome DevTools Protocol сообщения
- Запросы к браузеру
- Ответы браузера

Использование:
✓ Отладка автотестов (Puppeteer, Selenium)
✓ Понимание внутренней работы DevTools
✓ Разработка расширений
```

### 8. Recorder (Запись действий)

**Автоматизация тестов:**

```
More tools → Recorder → Start new recording

Записывает:
- Клики
- Ввод текста
- Навигацию
- Скроллинг

Экспорт:
- JSON
- Puppeteer script
- Puppeteer script (Replay)
- @playwright/test

Использование:
1. Записать user flow
2. Экспортировать в Puppeteer
3. Использовать как E2E тест
```

**Пример экспорта:**

```javascript
// Экспорт в Puppeteer
const puppeteer = require('puppeteer');

(async () => {
  const browser = await puppeteer.launch();
  const page = await browser.newPage();
  
  await page.goto('https://example.com');
  await page.click('#login-button');
  await page.type('#email', 'test@example.com');
  await page.type('#password', 'password123');
  await page.click('#submit');
  
  await browser.close();
})();
```

### 9. CSS Overview

**Анализ CSS:**

```
More tools → CSS Overview → Capture overview

Показывает:
- Количество элементов
- Используемые цвета
- Шрифты и размеры
- Медиа-запросы
- Проблемы контрастности

Использование:
✓ Аудит дизайн-системы
✓ Поиск несоответствий
✓ Проверка accessibility
✓ Оптимизация цветовой палитры
```

### 10. Accessibility Tree

**Как видят страницу screen readers:**

```
Elements → Accessibility pane

Или:
More tools → Accessibility

Показывает:
- Дерево доступности
- ARIA атрибуты
- Роли элементов
- Computed properties

Проверка:
✓ Каждый интерактивный элемент имеет имя
✓ Правильные роли (button, link, heading)
✓ Логический порядок (tab order)
✓ Alt текст для изображений
```

---

## Частые задачи тестировщика

### 1. Проверка формы

```javascript
// Полный чек-лист
// 1. Инспекция формы
const form = $('form');
console.log('Action:', form.action);
console.log('Method:', form.method);

// 2. Проверка всех полей
$$('input, textarea, select').forEach(field => {
  console.log({
    name: field.name,
    type: field.type,
    required: field.required,
    maxLength: field.maxLength,
    pattern: field.pattern,
    disabled: field.disabled
  });
});

// 3. Валидация
// Попробовать submit с пустыми обязательными полями
form.submit();

// Ввести невалидные данные
$('#email').value = 'not-an-email';
$('#phone').value = 'abc';

// Проверить сообщения об ошибках
console.log($$('.error-message').map(el => el.textContent));

// 4. Boundary testing
$('#age').value = '-1'; // минимум
$('#age').value = '150'; // максимум
$('#text').value = 'a'.repeat(1000); // длинный текст

// 5. XSS тестирование
$('#name').value = '<script>alert("XSS")</script>';
```

### 2. Проверка API запросов

```javascript
// 1. Фильтр API запросов
// Network → Fetch/XHR

// 2. Проверка конкретного запроса
// Клик на запрос → вкладки:
// - Headers (проверить Authorization)
// - Payload (отправленные данные)
// - Preview (ответ в удобном виде)
// - Response (сырой ответ)

// 3. Копирование запроса
// Правой кнопкой → Copy → Copy as cURL
// Вставить в терминал для повтора

// 4. Блокировка для тестирования ошибок
// Правой кнопкой → Block request URL
// Проверить, как приложение обрабатывает ошибку

// 5. Throttling
// Network → Slow 3G
// Проверить лоадеры и таймауты
```

### 3. Проверка адаптивности

```javascript
// Полный тест
const sizes = [
  {name: 'Mobile S', width: 320},
  {name: 'Mobile M', width: 375},
  {name: 'Mobile L', width: 425},
  {name: 'Tablet', width: 768},
  {name: 'Laptop', width: 1024},
  {name: 'Desktop', width: 1440}
];

sizes.forEach(size => {
  console.group(size.name);
  
  // Установить размер в Device Mode
  // Проверки:
  console.log('□ Горизонтальный скролл:', 
    document.body.scrollWidth > window.innerWidth);
  
  console.log('□ Текст читается:', 
    getComputedStyle(document.body).fontSize);
  
  console.log('□ Маленькие touch targets:',
    Array.from($$('a, button')).filter(el => {
      const rect = el.getBoundingClientRect();
      return rect.width < 48 || rect.height < 48;
    }).length
  );
  
  console.groupEnd();
});
```

### 4. Performance аудит

```javascript
// Snippet для быстрой проверки
(function performanceAudit() {
  const metrics = performance.timing;
  
  console.table({
    'DNS Lookup': metrics.domainLookupEnd - metrics.domainLookupStart,
    'TCP Connection': metrics.connectEnd - metrics.connectStart,
    'Request Time': metrics.responseStart - metrics.requestStart,
    'Response Time': metrics.responseEnd - metrics.responseStart,
    'DOM Processing': metrics.domComplete - metrics.domLoading,
    'Total Load Time': metrics.loadEventEnd - metrics.navigationStart
  });
  
  // Проверка ресурсов
  const resources = performance.getEntriesByType('resource');
  
  const slow = resources.filter(r => r.duration > 1000);
  console.warn(`Медленные ресурсы (>1s): ${slow.length}`);
  slow.forEach(r => console.log(r.name, (r.duration/1000).toFixed(2) + 's'));
  
  const large = resources.filter(r => r.transferSize > 500000);
  console.warn(`Большие файлы (>500KB): ${large.length}`);
  large.forEach(r => console.log(r.name, (r.transferSize/1024).toFixed(0) + 'KB'));
})();
```

### 5. Accessibility проверка

```javascript
// Полная проверка доступности
(function a11yCheck() {
  const issues = [];
  
  // 1. Изображения без alt
  $$('img:not([alt])').forEach(img => {
    issues.push({type: 'No alt', element: img});
  });
  
  // 2. Ссылки без href
  $$('a:not([href])').forEach(a => {
    issues.push({type: 'Link without href', element: a});
  });
  
  // 3. Кнопки без текста/label
  $$('button').forEach(btn => {
    if (!btn.textContent.trim() && !btn.getAttribute('aria-label')) {
      issues.push({type: 'Button without label', element: btn});
    }
  });
  
  // 4. Input без label
  $$('input').forEach(input => {
    const id = input.id;
    if (!id || !$(`label[for="${id}"]`)) {
      issues.push({type: 'Input without label', element: input});
    }
  });
  
  // 5. Низкий контраст (упрощенно)
  $$('*').forEach(el => {
    const style = getComputedStyle(el);
    const bg = style.backgroundColor;
    const color = style.color;
    // Детальную проверку см. в Accessibility panel
  });
  
  console.log(`Найдено проблем: ${issues.length}`);
  console.table(issues);
})();
```

---

## Вопросы на собеседовании

### Базовые вопросы

**1. Какие панели DevTools вы используете чаще всего?**

```
Ответ:
- Elements: проверка DOM и CSS
- Console: выполнение команд, просмотр ошибок
- Network: анализ запросов, производительности
- Application: проверка storage, cookies
- Sources: debugging JS
```

**2. Как найти элемент на странице через DevTools?**

```
Ответ:
1. Ctrl+Shift+C и кликнуть на элемент
2. Elements → Ctrl+F → ввести селектор
3. Console → $('селектор')
4. Правой кнопкой → Inspect
```

**3. Как проверить, почему элемент не кликается?**

```
Ответ:
1. Инспектировать элемент
2. Computed → проверить:
   - pointer-events: none
   - z-index (другой элемент поверх)
   - opacity: 0
   - visibility: hidden
3. Проверить disabled атрибут
4. Event Listeners → проверить обработчики
```

**4. Как посмотреть все запросы к API?**

```
Ответ:
1. Network → Fetch/XHR фильтр
2. Или в фильтре: method:POST domain:api
3. Preserve log если есть редиректы
4. Каждый запрос:
   - Headers: заголовки
   - Payload: тело запроса
   - Response: ответ
   - Timing: время выполнения
```

**5. Как эмулировать мобильное устройство?**

```
Ответ:
1. Ctrl+Shift+M (Device Mode)
2. Выбрать устройство или Responsive
3. Можно изменить:
   - Размер экрана
   - Device pixel ratio
   - User agent
   - Touch events
   - Geolocation
```

### Средние вопросы

**6. Как проверить производительность страницы?**

```
Ответ:
1. Performance → Reload and record
2. Анализ метрик:
   - FCP < 1.8s (First Contentful Paint)
   - LCP < 2.5s (Largest Contentful Paint)
   - TTI < 3.8s (Time to Interactive)
3. Network → проверить размер и количество ресурсов
4. Coverage → найти неиспользуемый код
5. Lighthouse → полный аудит
```

**7. Как найти утечку памяти?**

```
Ответ:
1. Performance → включить Memory
2. Записать сессию
3. График памяти должен стабилизироваться
4. Если растет — утечка
5. Memory → Heap snapshot:
   - Snapshot до и после действия
   - Comparison → искать Detached DOM
   - Искать забытые listeners, таймеры
```

**8. Как протестировать медленное соединение?**

```
Ответ:
1. Network → Throttling → Slow 3G
2. Проверить:
   - Показываются ли лоадеры
   - Есть ли таймауты
   - Корректна ли обработка ошибок
   - Не ломается ли UI
3. Можно создать кастомный профиль
4. Disable cache для чистого теста
```

**9. Как отладить JavaScript?**

```
Ответ:
1. Sources → найти файл
2. Breakpoint на нужной строке
3. Перезагрузить/выполнить действие
4. При остановке:
   - Scope: переменные
   - Watch: выражения
   - Call Stack: стек вызовов
   - Console: выполнить код
5. Step over/into/out для навигации
```

**10. Как проверить безопасность страницы?**

```
Ответ:
1. Security → проверить HTTPS, сертификат
2. Network → Headers:
   - Content-Security-Policy
   - X-Frame-Options
   - Strict-Transport-Security
3. Application → Cookies:
   - HttpOnly для auth
   - Secure
   - SameSite
4. Console → попробовать XSS:
   - Ввод <script>alert('XSS')</script>
5. Проверить localStorage на sensitive data
```

### Продвинутые вопросы

**11. Как найти причину медленной загрузки?**

```
Ответ:
1. Network → Waterfall:
   - Blocking → медленный DNS/TCP
   - Stalled → ожидание соединения
   - Request → долгий request
   - Waiting (TTFB) → медленный сервер
   - Download → большой файл
2. Performance → Main thread:
   - Долгий Parse HTML/CSS
   - Тяжелый JavaScript
3. Coverage → неиспользуемый код
4. Lighthouse → конкретные рекомендации
```

**12. Как протестировать PWA?**

```
Ответ:
1. Application → Manifest:
   - Проверить все поля
   - Иконки доступны
2. Service Workers:
   - Статус active
   - Offline работает
3. Cache Storage:
   - Ресурсы кэшируются
   - Стратегия кэша корректна
4. Network → Offline:
   - Приложение работает
5. Lighthouse → PWA аудит
```

**13. Что такое Lighthouse и как его использовать?**

```
Ответ:
Lighthouse — инструмент аудита web-страниц

Проверяет:
1. Performance (производительность)
2. Accessibility (доступность)
3. Best Practices (лучшие практики)
4. SEO
5. PWA

Использование:
1. DevTools → Lighthouse
2. Выбрать категории
3. Generate report
4. Анализ результатов:
   - Красный < 50 (плохо)
   - Желтый 50-90 (средне)
   - Зеленый > 90 (хорошо)
5. Следовать рекомендациям
```

**14. Как найти проблемы с CORS?**

```
Ответ:
1. Console → ошибка "CORS policy"
2. Network → запрос:
   - Status может быть (failed)
   - Нет Response Headers
3. Проверить Response Headers:
   - Access-Control-Allow-Origin
   - Access-Control-Allow-Methods
   - Access-Control-Allow-Headers
   - Access-Control-Allow-Credentials
4. Preflight (OPTIONS) запрос:
   - Должен быть успешным (200)
   - С правильными заголовками
5. Решение на сервере, не в браузере
```

**15. Как использовать DevTools для регрессионного тестирования?**

```
Ответ:
1. Recorder:
   - Записать user flow
   - Экспортировать в Puppeteer
   - Добавить проверки (assertions)
   - Запускать регулярно

2. Snippets:
   - Создать снипеты для проверок
   - Запускать на каждом релизе
   - Например: проверка форм, ссылок, изображений

3. Screenshots:
   - Full page screenshot
   - Сравнивать с эталоном (visual regression)

4. HAR файлы:
   - Сохранять Network traces
   - Сравнивать производительность

5. Overrides:
   - Тестировать фиксы локально
   - Перед деплоем
```

---

## Полезные ресурсы

### Официальная документация
- [Chrome DevTools Documentation](https://developer.chrome.com/docs/devtools/)
- [Firefox DevTools](https://developer.mozilla.org/en-US/docs/Tools)
- [Safari Web Inspector](https://webkit.org/web-inspector/)

### Тренировка
- [DevTools Tips](https://devtoolstips.org/)
- [Umaar's DevTools Tips](https://umaar.com/dev-tips/)

### Дополнительные инструменты
- [Lighthouse](https://developers.google.com/web/tools/lighthouse)
- [WebPageTest](https://www.webpagetest.org/)
- [Chrome DevTools Protocol](https://chromedevtools.github.io/devtools-protocol/)

---

## Заключение

Этот гайд покрывает большинство возможностей DevTools, необходимых для ежедневной работы тестировщика. Регулярная практика с этими инструментами позволит:

✓ Быстрее находить и локализовать баги
✓ Эффективнее тестировать frontend
✓ Лучше понимать работу веб-приложений
✓ Уверенно отвечать на собеседованиях
✓ Сотрудничать с разработчиками на одном языке

**Главный совет**: Открывайте DevTools каждый день и экспериментируйте!