{
  "name": "qa-sandbox",
  "version": "1.0.0",
  "description": "QA Sandbox - проект автотестов на Playwright + TypeScript",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui",
    "test:local": "BASE_URL=http://localhost:8000 playwright test",
    "test:local:headed": "BASE_URL=http://localhost:8000 playwright test --headed",
    "test:local:debug": "BASE_URL=http://localhost:8000 playwright test --debug",
    "test:local:ui": "BASE_URL=http://localhost:8000 playwright test --ui",
    "report": "playwright show-report",
    "codegen": "playwright codegen",
    "codegen:local": "BASE_URL=http://localhost:8000 playwright codegen http://localhost:8000/sandbox/forms.html"
  },
  "keywords": [
    "playwright",
    "typescript",
    "automation",
    "testing",
    "e2e",
    "qa-sandbox"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3"
  }
}
