# 🌍 Локальное vs Продакшн окружение

## Зачем это нужно?

При разработке и тестировании часто нужно запускать тесты на разных окружениях:

- **Локальное окружение** (`http://localhost:8000`) - для тестирования изменений до деплоя
- **Продакшн окружение** (`https://testingit.ru`) - для проверки работы на реальном сайте

## Как это работает?

### 1. Базовый URL в конфигурации

В файле `playwright.config.ts` мы используем переменную окружения:

```typescript
use: {
  // Можно переопределить через переменную окружения BASE_URL
  baseURL: process.env.BASE_URL || "https://testingit.ru",
}
```

**Что происходит:**
- Если переменная `BASE_URL` НЕ установлена → используется `https://testingit.ru`
- Если переменная `BASE_URL` установлена → используется её значение

### 2. Разные пути для разных окружений

В файле `FormsPage.ts` мы определяем правильный путь:

```typescript
async goto() {
  // Для локальной версии используем /sandbox/forms.html
  // Для продакшн версии используем /forms
  const isLocal = process.env.BASE_URL?.includes('localhost');
  const path = isLocal ? '/sandbox/forms.html' : '/forms';
  await this.page.goto(path);
  await this.page.waitForLoadState("networkidle");
}
```

**Почему разные пути?**
- **Продакшн**: файлы находятся по адресу `/forms`
- **Локально**: файлы находятся в папке `/sandbox/forms.html`

### 3. Удобные npm скрипты

В `package.json` добавлены команды с префиксом для базового URL:

```json
"scripts": {
  // Обычные команды (продакшн)
  "test": "playwright test",
  "test:headed": "playwright test --headed",
  
  // Команды для локального запуска
  "test:local": "BASE_URL=http://localhost:8000 playwright test",
  "test:local:headed": "BASE_URL=http://localhost:8000 playwright test --headed"
}
```

## Пошаговая инструкция

### Запуск тестов на продакшн

```bash
# Просто запусти тест как обычно
npm run test:headed
```

Playwright откроет `https://testingit.ru/forms`

### Запуск тестов на локальном сервере

**Шаг 1: Запусти локальный веб-сервер**

```bash
# Из корневой папки проекта qaed
python3 -m http.server 8000
```

Ты увидишь:
```
Serving HTTP on 0.0.0.0 port 8000 (http://0.0.0.0:8000/) ...
```

**Шаг 2: Открой новый терминал и запусти тесты**

```bash
# Перейди в папку с тестами
cd /Users/s-lebedev/qaed/sandbox/autotests

# Запусти тесты на локальной версии
npm run test:local:headed
```

Playwright откроет `http://localhost:8000/sandbox/forms.html`

## Как проверить, что работает локальная версия?

1. **Открой браузер** при запуске теста (используй `:headed` команды)
2. **Посмотри на URL** в адресной строке:
   - Локальная версия: `http://localhost:8000/sandbox/forms.html`
   - Продакшн версия: `https://testingit.ru/forms`

## Использование переменной окружения напрямую

Вместо npm скриптов можно задать переменную напрямую:

### macOS / Linux

```bash
BASE_URL=http://localhost:8000 npx playwright test --headed
```

### Windows (PowerShell)

```powershell
$env:BASE_URL="http://localhost:8000"; npx playwright test --headed
```

### Windows (CMD)

```cmd
set BASE_URL=http://localhost:8000 && npx playwright test --headed
```

## Расширение на другие окружения

Можно создать несколько окружений:

```json
"scripts": {
  "test:prod": "BASE_URL=https://testingit.ru playwright test",
  "test:staging": "BASE_URL=https://staging.testingit.ru playwright test",
  "test:local": "BASE_URL=http://localhost:8000 playwright test",
  "test:docker": "BASE_URL=http://localhost:3000 playwright test"
}
```

## Частые проблемы

### Проблема: Тесты падают с ошибкой "net::ERR_CONNECTION_REFUSED"

**Причина:** Локальный сервер не запущен

**Решение:**
```bash
# Запусти сервер в отдельном терминале
python3 -m http.server 8000
```

### Проблема: Открывается неправильный путь

**Причина:** В Page Object используется неправильный путь

**Решение:** Проверь, что в методе `goto()` правильно определяется путь:
```typescript
const isLocal = process.env.BASE_URL?.includes('localhost');
const path = isLocal ? '/sandbox/forms.html' : '/forms';
```

### Проблема: Тесты всё равно идут на продакшн

**Причина:** Переменная окружения не передаётся

**Решение:** Убедись, что используешь команду с `BASE_URL=`:
```bash
BASE_URL=http://localhost:8000 npx playwright test
# или
npm run test:local
```

## Преимущества такого подхода

✅ **Гибкость** - легко переключаться между окружениями  
✅ **Безопасность** - можно тестировать изменения локально до деплоя  
✅ **Скорость** - локальные тесты работают быстрее  
✅ **Отладка** - проще дебажить на локальной версии  
✅ **CI/CD готово** - можно настроить разные окружения в pipeline  

## Следующие шаги

1. Попробуй запустить тесты на обоих окружениях
2. Внеси изменения в `forms.html` локально
3. Запусти тесты на локальной версии и убедись, что они видят изменения
4. Задеплой изменения на продакшн
5. Запусти тесты на продакшн версии

Удачи! 🚀
