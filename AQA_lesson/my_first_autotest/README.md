# 🎭 Мой Первый Автотест на Playwright + TypeScript

Добро пожаловать в твой первый проект автоматизации тестирования! Здесь ты найдёшь всё необходимое для начала работы с Playwright.

**Тестируемый сайт:** [QA Sandbox (https://testingit.ru/)](https://testingit.ru/) - специальная песочница для практики автоматизации тестирования! 🎯

## 📋 Содержание

- [Что здесь?](#что-здесь)
- [Быстрый старт](#быстрый-старт)
- [Структура проекта](#структура-проекта)
- [Запуск тестов](#запуск-тестов)
- [Полезные команды](#полезные-команды)
- [Что дальше?](#что-дальше)

---

## 🎯 Что здесь?

Этот проект содержит:

- ✅ **Настроенный Playwright** с TypeScript
- ✅ **Page Object паттерн** для организации кода
- ✅ **Примеры тестов** с подробными комментариями
- ✅ **Готовую конфигурацию** для запуска тестов
- ✅ **Отчёты и скриншоты** при падении тестов

---

## 🚀 Быстрый старт

### 1. Установка зависимостей (если ещё не установлены)

```bash
npm install
```

### 2. Запуск тестов

```bash
# Запустить все тесты (headless режим)
npm test

# Запустить тесты с отображением браузера
npm run test:headed

# Запустить конкретный тест
npm test -- first.spec.ts

# Запустить в режиме отладки
npm run test:debug
```

### 3. Просмотр отчёта

```bash
npm run report
```

---

## 📁 Структура проекта

```
my_first_autotest/
│
├── pages/                      # Page Objects (представления страниц)
│   └── QASandboxPage.ts       # Page Object для главной страницы
│
├── tests/                      # Тесты
│   └── qa-sandbox.spec.ts     # Тесты главной страницы QA Sandbox (5 тестов)
│
├── test-results/              # Скриншоты тестов
│   ├── qa-sandbox-main-page.png    # Скриншот главной страницы
│   └── qa-sandbox-mobile.png       # Скриншот мобильной версии
│
├── playwright-report/         # HTML отчёты о выполнении
│
├── playwright.config.ts       # Конфигурация Playwright
├── tsconfig.json             # Конфигурация TypeScript
├── package.json              # Зависимости проекта
└── README.md                 # Этот файл
```

---

## 🎮 Запуск тестов

### Базовые команды

```bash
# Запустить все тесты
npx playwright test

# Запустить тесты в headed режиме (с открытым браузером)
npx playwright test --headed

# Запустить конкретный файл
npx playwright test first.spec.ts

# Запустить конкретный тест по названию
npx playwright test -g "Открытие главной страницы"

# Запустить в debug режиме
npx playwright test --debug
```

### Режимы выполнения

```bash
# UI Mode (интерактивный режим) - РЕКОМЕНДУЕТСЯ для обучения!
npx playwright test --ui

# Режим отладки (открывается Playwright Inspector)
npx playwright test --debug

# Запуск в определённом браузере
npx playwright test --project=chromium
```

---

## 🛠 Полезные команды

### Генерация кода (Codegen)

```bash
# Записать действия в браузере и получить код теста
npx playwright codegen https://www.google.com

# Codegen с эмуляцией мобильного устройства
npx playwright codegen --device="iPhone 13" https://www.google.com
```

### Отчёты и результаты

```bash
# Открыть HTML отчёт
npx playwright show-report

# Открыть trace для анализа упавшего теста
npx playwright show-trace test-results/traces/trace.zip
```

### Управление браузерами

```bash
# Установить все браузеры
npx playwright install

# Установить только Chromium
npx playwright install chromium

# Установить только Firefox
npx playwright install firefox
```

---

## 📝 Что в тестах?

### `tests/qa-sandbox.spec.ts` - **5 готовых автотестов** ✅

1. **✅ Открытие главной страницы** - проверка загрузки, URL, title
2. **✅ Проверка доступности разделов** - все ссылки видны и работают
3. **✅ Переход между страницами** - навигация по разделам
4. **✅ Мобильная версия** - тестирование на viewport 390x844 (iPhone)
5. **✅ Performance** - время загрузки страницы < 5 секунд

**Результаты последнего запуска:** 🎉  
✅ **5 тестов пройдено**  
⏱️ **Время выполнения: 7.1 секунд**  
📸 **2 скриншота создано**

---

## 🎓 Page Object Pattern

**Page Object** - это класс, который:
- Представляет одну страницу сайта
- Содержит локаторы элементов
- Содержит методы для работы со страницей

### Пример использования:

```typescript
// В Page Object определяем структуру
export class QASandboxPage {
  readonly alertsLink: Locator;
  
  constructor(page: Page) {
    this.alertsLink = page.getByRole('link', { name: /alerts/i });
  }
  
  async goToAlerts() {
    await this.alertsLink.click();
    await this.page.waitForURL(/.*alerts.*/);
  }
}

// В тесте просто используем
const sandboxPage = new QASandboxPage(page);
await sandboxPage.goToAlerts();
```

### Преимущества:
✅ Код тестов читается как сценарий на человеческом языке  
✅ Если UI изменится, правим только Page Object  
✅ Нет дублирования селекторов  
✅ Легко поддерживать  

---

## 🔍 Типы селекторов в Playwright

Playwright рекомендует использовать **семантические селекторы**:

```typescript
// ✅ ХОРОШО - семантические селекторы
page.getByRole('button', { name: 'Submit' })
page.getByText('Welcome')
page.getByLabel('Email')
page.getByPlaceholder('Enter your name')
page.getByTestId('submit-button')

// ⚠️ НЕ РЕКОМЕНДУЕТСЯ - хрупкие селекторы
page.locator('.btn-primary')  // CSS классы могут измениться
page.locator('#submit')        // ID могут измениться
page.locator('//button[@id="submit"]')  // XPath сложен в поддержке
```

---

## 📊 Отчёты и диагностика

### HTML Report

После запуска тестов автоматически создаётся HTML отчёт:

```bash
npx playwright show-report
```

Отчёт показывает:
- ✅ Пройденные тесты
- ❌ Упавшие тесты
- ⏱ Время выполнения
- 📸 Скриншоты (если тест упал)
- 🎥 Видео (если настроено)

### Trace Viewer

Trace Viewer - это "машина времени" для тестов:

```bash
npx playwright show-trace trace.zip
```

Показывает:
- Все действия теста
- DOM состояние на каждом шаге
- Network запросы
- Console логи
- Скриншоты каждого действия

---

## 🐛 Отладка тестов

### Способ 1: Debug Mode

```bash
npx playwright test --debug
```

Открывает Playwright Inspector:
- Пошаговое выполнение
- Просмотр локаторов
- Выполнение команд в реальном времени

### Способ 2: UI Mode (ЛУЧШИЙ для обучения!)

```bash
npx playwright test --ui
```

Интерактивный режим:
- Видишь все тесты
- Запускаешь любой тест
- Смотришь trace в реальном времени
- Изменяешь и перезапускаешь

### Способ 3: Консольный вывод

Добавляй `console.log()` в тесты:

```typescript
test('Мой тест', async ({ page }) => {
  console.log('🔍 Открываю страницу...');
  await page.goto('https://example.com');
  
  console.log('✅ Страница открыта!');
});
```

### Способ 4: Pause тест

```typescript
test('Мой тест', async ({ page }) => {
  await page.goto('https://example.com');
  
  // Остановить выполнение для инспекции
  await page.pause();
  
  await page.click('button');
});
```

---

## 🎯 Best Practices (Лучшие практики)

### 1. Используй Page Object Pattern
```typescript
// ✅ ХОРОШО
const loginPage = new LoginPage(page);
await loginPage.login('user', 'pass');

// ❌ ПЛОХО
await page.fill('#username', 'user');
await page.fill('#password', 'pass');
await page.click('#login-button');
```

### 2. Делай независимые тесты
```typescript
// ✅ ХОРОШО - каждый тест независим
test('Test 1', async ({ page }) => {
  await page.goto('/');
  // Тест 1 не зависит от других
});

test('Test 2', async ({ page }) => {
  await page.goto('/');
  // Тест 2 не зависит от других
});
```

### 3. Используй семантические селекторы
```typescript
// ✅ ХОРОШО
page.getByRole('button', { name: 'Submit' })

// ❌ ПЛОХО
page.locator('.btn-123-abc')
```

### 4. Добавляй осмысленные проверки
```typescript
// ✅ ХОРОШО
await expect(page.getByText('Success!')).toBeVisible();
await expect(page).toHaveURL(/dashboard/);

// ❌ ПЛОХО
await page.waitForTimeout(3000); // Ждём просто так
```

### 5. Группируй связанные тесты
```typescript
test.describe('Login functionality', () => {
  test('successful login', async ({ page }) => { });
  test('failed login', async ({ page }) => { });
  test('logout', async ({ page }) => { });
});
```

---

## 🚀 Что дальше?

### Изучи документацию:
- 📖 [Официальная документация Playwright](https://playwright.dev)
- 📖 [Best Practices](https://playwright.dev/docs/best-practices)
- 📖 [API Reference](https://playwright.dev/docs/api/class-playwright)
- 🧪 [QA Sandbox - тренировочная площадка](https://testingit.ru/)

### Попробуй:
1. ✏️ Напиши тесты для других разделов QA Sandbox (Формы, Таблицы, Drag & Drop)
2. 🎨 Добавь новый Page Object для раздела "Модальные окна"
3. 🔄 Настрой CI/CD (GitHub Actions)
4. 📱 Протестируй мобильную версию всех разделов
5. 🌐 Добавь тесты для API (если есть на сайте)

### Расширь проект:
```bash
# Добавь больше браузеров в playwright.config.ts
# Настрой параллельное выполнение
# Добавь fixtures для повторяющейся логики
# Интегрируй с Allure для красивых отчётов
```

---

## 💡 Частые вопросы (FAQ)

### Q: Тест падает с ошибкой "Element not found"
**A:** Увеличь таймаут или проверь селектор:
```typescript
await element.waitFor({ timeout: 10000 });
```

### Q: Как запустить тест на production и staging?
**A:** Используй переменные окружения:
```typescript
const baseURL = process.env.BASE_URL || 'https://staging.example.com';
```

### Q: Как протестировать API?
**A:** Playwright умеет это из коробки:
```typescript
const response = await page.request.get('https://api.example.com/data');
expect(response.status()).toBe(200);
```

### Q: Тесты работают медленно
**A:** Включи параллельное выполнение в `playwright.config.ts`:
```typescript
workers: 4  // Количество параллельных процессов
```

---

## 📞 Поддержка

Если что-то не работает:
1. Проверь, установлены ли браузеры: `npx playwright install`
2. Обнови зависимости: `npm install`
3. Почитай документацию: https://playwright.dev
4. Запусти в debug режиме: `npx playwright test --debug`
5. Изучи [QA Sandbox](https://testingit.ru/) для практики

---

## 🎉 Поздравляю!

Ты создал свой первый проект с автотестами! Теперь у тебя есть:
- ✅ Рабочий проект на Playwright + TypeScript
- ✅ Понимание Page Object Pattern
- ✅ Примеры различных типов тестов
- ✅ Знание основных команд и инструментов

**Продолжай практиковаться и автоматизировать!** 🚀

---

*Создано с ❤️ для изучения автоматизации тестирования*

