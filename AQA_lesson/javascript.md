# Конспект лекции: Основы JavaScript

## 📋 Введение

**JavaScript** - самый популярный язык программирования в мире:
- Используется почти на всех веб-сайтах
- Обеспечивает интерактивность сайтов: всплывающие окна, сложная логика
- Не сложный в изучении (практика прямо в браузере)
- Применим для QA в написании автотестов

## 📚 Краткая история

- **Создан**: 1995 год, за 10 дней
- **Автор**: Брендан Эйх (Netscape)
- **Первоначальное название**: LiveScript → JavaScript
- **Современное применение**: 
  - Веб-браузеры
  - Мобильные приложения
  - Серверы
  - Десктопные программы
  - Игры

## 🛠 Настройка рабочей среды

1. Откройте любой браузер (Chrome, Firefox, Safari)
2. Нажмите `F12` или `Ctrl+Shift+I` (Mac: `Cmd+Option+I`)
3. Перейдите во вкладку **Console**
4. Копируйте примеры и нажимайте `Enter`

ИЛИ
Используйте IDE, я рекомендую использовать [Visual Studio Code](https://code.visualstudio.com/) она бесплатная и подходит для любых языков программирования
скачивай и открывай лекцию в IDE там же пробуй запускать примеры

## 🚀 Первые шаги

### Первая программа
```javascript
alert("Привет, мир!");
```

### Вывод в консоль
```javascript
console.log("Я программирую на JavaScript!");
```

## 📦 Переменные - это способ сохранить информацию под понятным именем, чтобы использовать её позже!

### Способы объявления переменных:
- `var` - **НЕ ИСПОЛЬЗУЕМ** (устаревший, создает ошибки)
- `let` - если значение будет меняться
- `const` - если значение не меняется

### Примеры:
```javascript
let name = "Алексей";
console.log(name);

let age = 25;
console.log(age);

const city = "Москва";
console.log(city);
```

### 💡 Практическое задание
Создать переменную со своим именем и вывести приветственное сообщение:
```javascript
let myName = "Сергей";
console.log("Привет, " + myName);
```

## 🔢 Типы данных

### Основные типы (90% случаев):
```javascript
let number = 42;           // Число
let text = "Привет";       // Строка
let isActive = true;       // Булево значение
```

### Проверка типа данных:
```javascript
console.log(typeof 42);        // "number"
console.log(typeof "Привет");  // "string"
console.log(typeof true);      // "boolean"
```
typeof - это оператор, который показывает, какого типа данные хранятся в переменной.

## ⚙️ Функции - это блок кода, который можно использовать много раз, просто вызывая его по имени.

### Зачем нужны функции?

**1. Избежать повторения кода**

Без функции ❌:
```javascript
console.log("Привет, Иван!");
console.log("Привет, Мария!");
console.log("Привет, Петр!");
```

С функцией ✅:
```javascript
function sayHello(name) {
    console.log("Привет, " + name + "!");
}

sayHello("Иван");   // Привет, Иван!
sayHello("Мария");  // Привет, Мария!
sayHello("Петр");   // Привет, Петр!
```

### Как создать функцию?

```javascript
// Объявление функции
function имяФункции(параметры) {
    // тело функции и код, который выполнится
    return результат;
}

// Вызов функции
имяФункции(аргументы);
```

### Примеры

**1. Простая функция без параметров**
```javascript
function sayHi() {
    console.log("Привет!");
}

sayHi(); // Привет!
sayHi(); // Привет!
```

**2. Функция с параметрами**
```javascript
function add(a, b) {
    return a + b;
}

let sum = add(5, 3);  // 8
console.log(sum);     // 8
```

### Разные способы создания функций

**1. Объявление функции**
```javascript
function greet() {
    return "Привет!";
}
```

**2. Функция-выражение**
```javascript
let greet = function() {
    return "Привет!";
};
```

**3. Стрелочная функция (современный способ)**
```javascript
let greet = () => {
    return "Привет!";
};

// Или короче
let greet = () => "Привет!";
```

### Практический пример для закрепления

```javascript
function greet(name) {
  return "Привет, " + name + "!";
}

let your_name = greet("Твое имя");
console.log(your_name);
```
ДЗ для тебя: преобразуй в стрелочную функцию


## 🔍 Операторы сравнения

```javascript
// Равенство
5 == 5          // true (равно по значению)
5 === 5         // true (равно по значению И типу)
5 == "5"        // true (приводит "5" к числу)
5 === "5"       // false (разные типы!)

// Не равно
5 != 3          // true
5 !== "5"       // true (разные типы)

// Больше/меньше
10 > 5          // true
10 < 5          // false
10 >= 10        // true (больше или равно)
5 <= 10         // true (меньше или равно)
```
Важно: Всегда используйте === и !== для избежания ошибок!

5. Логические операторы
&& (И) - оба условия должны быть true
```
let age = 25;
let hasLicense = true;

if (age >= 18 && hasLicense) {
    console.log("Можете водить машину");
}
// true, потому что ОБА условия выполнены
```
|| (ИЛИ) - хотя бы одно условие true
```
let isWeekend = true;
let isHoliday = false;

if (isWeekend || isHoliday) {
    console.log("Можно отдыхать!");
}
// true, потому что хотя бы ОДНО условие выполнено
```
! (НЕ) - инверсия
```
let isRaining = false;

if (!isRaining) {
    console.log("Можно идти гулять");
}
// true, потому что !false = true
```

## 🔄 Условия и логика - это способ принимать решения в коде.

"Если это правда, делай одно, иначе делай другое".
🚦 Если горит зелёный свет → иди
Иначе → стой

1. IF - основа условий

```javascript
let age = 20;

if (age >= 18) {
    console.log("Вы совершеннолетний");
}
```
"Если возраст больше или равен 18, выведи сообщение"
2. IF...ELSE - два варианта
```javascript
// Обычный if-else
let age = 20;
let status;

if (age >= 18) {
    status = "Взрослый";
} else {
    status = "Ребёнок";
}

// То же самое, но короче (тернарный оператор)
let status = age >= 18 ? "Взрослый" : "Ребёнок";
```
3. IF...ELSE IF...ELSE - много вариантов
```javascript
let score = 85;

if (score >= 90) {
    console.log("Отлично! 🌟");
} else if (score >= 70) {
    console.log("Хорошо! 👍");
} else if (score >= 50) {
    console.log("Удовлетворительно");
} else {
    console.log("Нужно подучить");
}
// Выведет: "Хорошо! 👍"
```

## 🧮 Практический пример: Калькулятор

```javascript
function calculator() {
    let num1 = parseInt(prompt("Первое число:"));
    let operation = prompt("Операция (+, -, *, /):");
    let num2 = parseInt(prompt("Второе число:"));
    
    let result;
    
    if (operation === '+') result = num1 + num2;
    else if (operation === '-') result = num1 - num2;
    else if (operation === '*') result = num1 * num2;
    else if (operation === '/') result = num1 / num2;
    else {
        alert("Неизвестная операция!");
        return;
    }
    
    alert(`Результат: ${result}`);
}

calculator();
```
4. Switch - много вариантов выбора

```javascript
let day = "понедельник";

switch (day) {
    case "понедельник":
        console.log("Начало недели");
        break;
    case "пятница":
        console.log("Почти выходные!");
        break;
    case "суббота":
    case "воскресенье":
        console.log("Выходной!");
        break;
    default:
        console.log("Обычный день");
}
```

## 📝 Массивы и циклы

### Практический пример: Todo-лист
```javascript
function createTodoList() {
    let todos = [];
    
    // Цикл для ввода задач
    for (let i = 0; i < 3; i++) {
        let todo = prompt(`Введи задачу ${i + 1}:`);
        todos.push(todo);
    }
    
    // Вывод списка дел
    console.log("Твой список дел:");
    for (let i = 0; i < todos.length; i++) {
        console.log(`${i + 1}. ${todos[i]}`);
    }
}

createTodoList();
```

### Методы массивов:
```javascript
let nums = [1, 2, 3, 4, 5];

// forEach - выполнить для каждого элемента
nums.forEach(num => console.log(num * 2));

// filter - отфильтровать элементы
let evenNumbers = nums.filter(num => num % 2 === 0);
console.log(evenNumbers); // [2, 4]

// map - преобразовать элементы
let doubled = nums.map(num => num * 2);
console.log(doubled); // [2, 4, 6, 8, 10]
```

## 🤖 Автоматизация: Автокликер

```javascript
function autoClicker() {
    // Найти все кнопки на странице
    let buttons = document.querySelectorAll('button');
    
    if (buttons.length === 0) {
        alert("Кнопки не найдены!");
        return;
    }
    
    console.log(`Найдено кнопок: ${buttons.length}`);
    
    // Нажать на каждую кнопку с интервалом
    buttons.forEach((button, index) => {
        setTimeout(() => {
            button.click();
            console.log(`Нажата кнопка ${index + 1}: ${button.textContent}`);
        }, index * 1000); // Задержка в 1 секунду между кликами
    });
}

autoClicker();
```

## 📚 Что изучили:

✅ **Переменные** (`let`, `const`) и типы данных  
✅ **Функции** (обычные и стрелочные)  
✅ **Условия** (`if`, `else`, `switch`) и операторы   
✅ **Циклы** (`for`) и работа с массивами  
✅ **Методы массивов** (`forEach`, `filter`, `map`)  
✅ **Работа с DOM** и браузером  
✅ **Практические примеры** для QA автоматизации  

## 🏠 Домашнее задание

### Задание 1: Улучшить автокликер
Доработать автокликер так, чтобы он выводил в консоль все ссылки, на которые кликали.

### Задание 2: Генератор паролей
**Техническое задание:**
- Генерировать пароль из заглавных и строчных букв латинского алфавита + цифры
- Длину пароля вводит пользователь
- Результат выводится в консоль браузера

**Подсказка для реализации:**
```javascript
function generatePassword() {
    const chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    // Ваш код здесь...
}
```

## 🎯 Что дальше?

1. **Практикуйтесь** - открывайте разные сайты, экспериментируйте в консоли
2. **Создавайте проекты** - генераторы, автоматизация, игры
3. **Изучайте дальше** - фреймворки, библиотеки, серверная разработка

---
