# 🚀 Деплой песочницы testingit.ru на Netlify

## Шаг 1: Подготовка репозитория

```bash
cd /Users/s-lebedev/qaed

# Добавляем все файлы в git
git add .

# Коммитим изменения
git commit -m "Добавлена песочница для автоматизации тестирования"

# Пушим в GitHub
git push origin main
```

> ⚠️ Убедитесь, что у вас есть репозиторий на GitHub!  
> Если нет, создайте:
> ```bash
> # Если репозитория нет
> git remote add origin https://github.com/ваш-username/qaed.git
> git branch -M main
> git push -u origin main
> ```

---

## Шаг 2: Регистрация и деплой на Netlify

### 2.1. Регистрация

1. Перейдите на https://www.netlify.com
2. Нажмите **"Sign up"**
3. Выберите **"Sign up with GitHub"** (проще всего)
4. Авторизуйте Netlify доступ к GitHub

### 2.2. Создание нового сайта

1. На главной странице Netlify нажмите **"Add new site"** → **"Import an existing project"**

2. Выберите **"Deploy with GitHub"**

3. Найдите и выберите репозиторий **qaed**

4. Настройте параметры деплоя:
   ```
   Branch to deploy: main
   Base directory: (оставьте пустым)
   Build command: (оставьте пустым - у нас статический сайт)
   Publish directory: sandbox
   ```

5. Нажмите **"Deploy site"**

6. ✅ Через 30-60 секунд ваш сайт будет доступен по адресу:
   ```
   https://random-name-12345.netlify.app
   ```

---

## Шаг 3: Подключение домена testingit.ru

### 3.1. В Netlify Dashboard

1. Откройте ваш сайт в Netlify
2. Перейдите в **"Site settings"** → **"Domain management"**
3. Нажмите **"Add custom domain"**
4. Введите: `testingit.ru`
5. Нажмите **"Verify"** → **"Add domain"**

### 3.2. Настройка DNS у регистратора

Netlify покажет вам DNS записи. Скорее всего это будет:

#### Вариант A (рекомендуется - Netlify DNS):

Netlify предложит делегировать DNS на свои серверы:

```
ns1.netlify.com
ns2.netlify.com
ns3.netlify.com
ns4.netlify.com
```

**У вашего регистратора домена (где купили testingit.ru):**
1. Найдите раздел "DNS" или "Name Servers"
2. Замените NS-записи на серверы Netlify
3. Сохраните изменения

#### Вариант B (если не хотите менять NS):

Добавьте следующие записи у регистратора:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| CNAME | @ | ваш-сайт.netlify.app | 3600 |
| CNAME | www | ваш-сайт.netlify.app | 3600 |

> ⚠️ Некоторые регистраторы не поддерживают CNAME для корневого домена (@).  
> В этом случае используйте:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 75.2.60.5 | 3600 |
| CNAME | www | ваш-сайт.netlify.app | 3600 |

### 3.3. Ожидание применения DNS

- Обычно DNS применяется за **15-30 минут**
- Максимум может занять **24 часа**
- Проверить статус можно в Netlify Dashboard

### 3.4. Автоматический HTTPS

1. После применения DNS, Netlify **автоматически** выпустит SSL сертификат
2. Это займёт несколько минут
3. В разделе **"Domain management" → "HTTPS"** появится статус:
   ```
   ✅ HTTPS enabled
   Certificate: Let's Encrypt
   ```

---

## Шаг 4: Проверка работы

1. Откройте https://testingit.ru
2. Убедитесь, что:
   - ✅ Сайт загружается
   - ✅ HTTPS работает (замочек в браузере)
   - ✅ Все ссылки работают
   - ✅ Переходы между страницами работают

---

## 🎉 Готово!

Теперь при каждом `git push` в ветку `main`:
- Netlify **автоматически** задеплоит изменения
- Сборка занимает ~30 секунд
- Изменения появятся на https://testingit.ru

---

## 📊 Дополнительные возможности Netlify

### 1. Deploy Previews

Для каждого Pull Request Netlify создаёт отдельный preview:
- URL вида: `deploy-preview-123--ваш-сайт.netlify.app`
- Можно протестировать изменения до мержа

### 2. Form Handling

В HTML формах добавьте атрибут `netlify`:

```html
<form netlify>
  <input type="email" name="email">
  <button type="submit">Submit</button>
</form>
```

Netlify будет собирать отправки в Dashboard.

### 3. Redirects и переадресация

Уже настроено в `netlify.toml`:
- SPA редиректы работают
- 404 страницы перенаправляются на главную

### 4. Аналитика

В Netlify Dashboard можно включить:
- **Analytics** - посещаемость (платно $9/мес)
- **Forms** - отправки форм (100 бесплатно)

---

## 🔧 Полезные команды

```bash
# Проверить DNS
dig testingit.ru
nslookup testingit.ru

# Проверить HTTPS
curl -I https://testingit.ru

# Локальная проверка
cd /Users/s-lebedev/qaed/sandbox
python3 -m http.server 8080
# Откройте http://localhost:8080
```

---

## ❓ Частые проблемы

### DNS не применился

**Решение:**
1. Очистите DNS кэш:
   ```bash
   # macOS
   sudo dscacheutil -flushcache
   sudo killall -HUP mDNSResponder
   ```
2. Проверьте DNS через https://dnschecker.org

### HTTPS не работает

**Решение:**
1. Дождитесь полного применения DNS
2. В Netlify Dashboard → HTTPS → нажмите "Verify DNS configuration"
3. Если не помогло, нажмите "Renew certificate"

### 404 ошибка

**Решение:**
- Проверьте, что в настройках Netlify указано:
  - Publish directory: `sandbox`
- Проверьте, что файл `netlify.toml` закоммичен в git

---

## 📞 Нужна помощь?

- Документация Netlify: https://docs.netlify.com
- Чат поддержки: https://t.me/+NT-IOeLALxszN2Zi (ваш чат взаимопомощи)
- GitHub Issues: создайте issue в репозитории

---

**Автор:** Сергей Лебедев | QA  
**Telegram:** https://t.me/qabigtech  
**YouTube:** https://www.youtube.com/@qabigtech

